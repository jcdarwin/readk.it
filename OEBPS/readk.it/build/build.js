({
    appDir: '..',
    baseUrl: '.',
    dir: './dist',
    modules: [
        {
            name: 'js/main'
        }
    ],
    paths: {
    },
    fileExclusionRegExp: /^\.|^html|^build|^test|^example|\.orig$|^offline.manifest|\.md$/
})
